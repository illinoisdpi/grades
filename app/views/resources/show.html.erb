<div class="container my-5">
  <div>
    <h2 class="text-xl font-bold text-gray-900 mb-6">
      📌 <%= @resource.project_path %>
    </h2>

    <ol class="space-y-6 list-decimal ml-4 text-gray-700">
      <li>
        <strong>Fork the project</strong>:
        <a href="<%= @resource.project_url %>/fork" target="_blank">Click here copy the starter code to your own account.</a><br>
        <span class="text-sm text-gray-500">
          Only fork <strong>once</strong> per project. If you already forked, find your project at:
          <code class="bg-gray-100 p-1 rounded">github.com/your-username/project-name</code>
        </span>
      </li>

      <li>
        <strong>Set up your IDE (Integrated Development Environment)</strong>: Create an IDE to write and preview your code:
        <ul class="list-disc ml-4">
          <li>
            <%# TODO: add instruction to create codespace on main %>
            <a href="https://github.com/codespaces" target="_blank">Codespaces</a><br>
          </li>
        </ul>
      </li>

      <li>
        <strong>Read the project README</strong>:
        <a href="<%= @resource.project_url %>" target="_blank">Click here for the project README with instructions.</a>
      </li>

      <li>
        <strong>Automated grading feedback</strong>: When you're ready to get feedback, run this in your terminal:
        <div class="mt-2">
          <code class="bg-gray-200 p-2 rounded font-mono">rake grade</code>
        </div>
        <p class="text-sm mt-2">
          You can run <code class="bg-gray-100 p-1 rounded">rake grade</code> as many times as you like. Only your highest score will be recorded as a grade.
        </p>

        <p class="mt-3">
          <%# TODO: add auto copy snippet button %>
          You may be asked for an access token when running <code>rake grade</code>. When this happens copy and paste: <code class="bg-gray-200 p-2 rounded font-mono"><%= @launch.submission_token %></code>
        </p>

        <p class="text-sm mt-3 text-gray-500">
          You can reset your access token by running: <code class="bg-gray-200 p-2 rounded font-mono">rake grade:reset_token</code>
        </p>
        
      </li>
    </ol>

    <% if @builds.any? %>
      <div class="mt-8">
        <strong>Current score:</strong> <%= number_to_percentage((@builds.highest_score * 100), precision: 0) %>
        <div class="mt-1 bg-gray-200 rounded h-2">
          <div class="bg-gray-500 rounded h-2" style="width: 0%"></div>
        </div>
      </div>
    <% end %>
  </div>

  <%= render "builds/list_group", builds: @builds%>
</div>
